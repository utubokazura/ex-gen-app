<!DOCTYPE html>
<html lang="ja">
    <head>
        <meta http-equiv="content-type"
            content="text/html; charset=UTF-8">
        <title><%= title %></title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/
            bootstrap.css"
            rel="stylesheet" crossorigin="anonymous">
        <link rel='stylesheet' href='/stylesheets/style.css' />
    </head>

    <script>
        function DeleteAcount(id) {
            var url = "http://localhost:3000/users/delete/"+ id;
            window.location.href = url;
        }
    </script>


    <body class="container">
       
        <header>
            <h1 class="display-4">
                チャット画面
            </h1>
        </header>

        <script>
            var mail = '<%= login.mail %>';
        </script>
        
        <div role="main">
            <p class="h4"><%= login.name %>さん</p>
            <p class="h4"><%= login.mail %></p>
          

            <table class="table mt-5">
                <% for(let i = content.length - 1; i >= 0; i--) { %>
                    <%- include('data_item', { val: content[i] }) %>
                <% } %>
            </table>

            <form method="post" action="/boards/add">
                <div class="row">
                    <div class="col-10">
                        <input type="text" size="100" name="msg"
                            class="form-control">
                        <input type="submit" value="送信"
                            class="btn btn-primary col-2">
                    </div>
                </div>
            </form>
         <!-- <ul class="pagination justify-content-center">
                <li class="page-item">
                    <a href="/boards/<%= page - 1 %>"
                        class="page-link">&lt;&lt; prev</a>
                </li>
                <li class="page-item">
                    <a href="/boards/<%= page + 1 %>"
                        class="page-link">Next &gt;&gt;</a>
                </li>
            </ul> 
        </div>-->
        <div>
            <button onclick="location.href='https://ex-gen-app-223e8fdda03f.herokuapp.com/users'">ユーザーリスト</button>
        </div>
    <!--<div>
            <button onclick="location.href='http://localhost:3000/users/calender'">スケジュール表示</button>
        </div> -->
     <!--   <form method="post" action="/users/calender">
            <div class="form-group">
                <label for="mail">Googleアカウントのメールアドレスを入力</label>
                <input type="text" name="mail" id="mail"
                    class="form-control">
            <input type="submit" value="スケジュールを表示"
            class="btn btn-primary">
        </div>
        </form>-->
        <!--
        <div>
            <button onclick="DeleteAcount('<%= login.id %>')">アカウント削除</button>
        </div>
        -->
        <div>
            <button onclick="location.href='https://ex-gen-app-223e8fdda03f.herokuapp.com/users/login'">ログアウト</button>
        </div>
      
    </body>
</html>
