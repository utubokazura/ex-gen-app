<!DOCTYPE html>
<html lang="ja">

<head>
    <meta http-equiv="content-type"
        content="text/html; charset=UTF-8">
    <title><%= title %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/
        bootstrap.css"
        rel="stylesheet" crossorigin="anonymous">
    <link rel='stylesheet'
        href="stylesheets/style.css" />
</head>

<script>
    function SendMailData(address) {
        var url = "http://localhost:3000/users/calender/?mail=" + address;
        window.location.href = url;
    }
</script>

<body class="container">

    <header>
        <h1 class="display-4">
            ユーザーリスト
        </h1>
    </header>
    <div role="main">
        <table class="table">
            <% for(var obj of content) { %>
            <tr>
                <th><%= obj.id %></th>
                <td><%= obj.name %></td>
                <td><%= obj.mail %></td>
                <td><button onclick="SendMailData('<%= obj.mail %>')">📅</button></td>
                <td><button onclick="location.href='http://localhost:8000/'">スケジュールの変更</button></td>
            </tr>
        <% } %>
        </table>
    </div>
</body>

</html>